name: Publish Android AAR to Maven

on:
  workflow_call:
    inputs:
      version:
        description: "Maven version to publish"
        required: true
        type: string

concurrency:
  group: release-android-publish-${{ inputs.version }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

env:
  NDK_VERSION: "29.0.14206865"
  SNES9X_ABIS: "arm64-v8a,x86_64"

jobs:
  publish:
    name: Verify and publish Maven package
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true
          fetch-depth: 1
      - name: Validate version
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z][0-9A-Za-z.-]*)?$ ]]; then
            echo "::error::Invalid Maven version: $VERSION"
            exit 1
          fi
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: android-actions/setup-android@v4
      - name: Install NDK
        run: |
          sdkmanager --install "ndk;${NDK_VERSION}"
          echo "ANDROID_NDK_ROOT=${ANDROID_SDK_ROOT}/ndk/${NDK_VERSION}" >> "$GITHUB_ENV"
      - uses: gradle/actions/setup-gradle@v6
      - name: Build release AAR
        run: |
          ./gradlew --no-daemon :snes9x:assembleRelease \
            -Psnes9x.abis="${SNES9X_ABIS}" \
            -Psnes9x.version="${{ inputs.version }}"
      - name: Build sample from release AAR
        run: |
          ./gradlew --no-daemon :app:assembleLocalRelease \
            -Psnes9x.releaseAar="${GITHUB_WORKSPACE}/android/snes9x/build/outputs/aar/snes9x-release.aar" \
            -Psnes9x.abis="${SNES9X_ABIS}"
      - name: Verify release artifacts
        run: |
          scripts/verify-release-artifacts.sh \
            android/snes9x/build/outputs/aar/snes9x-release.aar \
            android/app/build/outputs/apk/local/release/snes9x-local-release-unsigned.apk \
            "${SNES9X_ABIS}"
      - name: Bundle complete source
        env:
          VERSION: ${{ inputs.version }}
        run: |
          SOURCE_ARCHIVE="${GITHUB_WORKSPACE}/android/snes9x/build/distributions/snes9x-${VERSION}-complete-source.tar.gz"
          scripts/package-source-archive.sh "$SOURCE_ARCHIVE" "snes9x-${VERSION}-source"
          echo "SNES9X_COMPLETE_SOURCE_ARCHIVE=${SOURCE_ARCHIVE}" >> "$GITHUB_ENV"
      - name: Publish to GitHub Packages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew --no-daemon :snes9x:publishReleasePublicationToGitHubPackagesRepository \
            -Psnes9x.abis="${SNES9X_ABIS}" \
            -Psnes9x.version="${{ inputs.version }}" \
            -Psnes9x.completeSourceArchive="${SNES9X_COMPLETE_SOURCE_ARCHIVE}" \
            -Psnes9x.githubRepository="${GITHUB_REPOSITORY}"
      - name: Write provenance
        run: |
          {
            echo "coordinates=io.github.dooop:snes9x:${{ inputs.version }}"
            echo "repository=${GITHUB_REPOSITORY}"
            echo "wrapper_commit=${GITHUB_SHA}"
            echo "upstream_commit=$(git -C snes9x rev-parse HEAD)"
            echo "abis=${SNES9X_ABIS}"
            echo "ndk=${NDK_VERSION}"
          } > android-release-provenance.txt
      - uses: actions/upload-artifact@v7
        with:
          name: snes9x-android-${{ inputs.version }}
          path: |
            android/snes9x/build/outputs/aar/snes9x-release.aar
            android/snes9x/build/publications/release/pom-default.xml
            android/snes9x/build/intermediates/source_jar/release/release-sources.jar
            android/snes9x/build/distributions/snes9x-${{ inputs.version }}-complete-source.tar.gz
            android/app/build/outputs/apk/local/release/snes9x-local-release-unsigned.apk
            android-release-provenance.txt
          if-no-files-found: error
